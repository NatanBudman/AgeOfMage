using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElfTalking : MonoBehaviour
{
    // Start is called before the first frame update
    public static bool Standart;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
